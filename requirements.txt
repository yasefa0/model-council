anthropic>=0.40.0
openai>=1.50.0
httpx[http2]>=0.27.0     # direct dep — used for tuned AsyncClient pool
stamina>=24.2.0
typer>=0.12.0
rich>=13.7.0
prompt_toolkit>=3.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
# Optional — enables MCP server mode (--mcp flag)
fastmcp>=3.0.0
